import React from "react";
import PropTypes from "prop-types";


export const FooterWrapper = (props) => {
    const {} = props;

    return (
        <footer className={"footer"}>
            <p className={"text-center footer--text"}>By Yang Jin Je @ Jellicle</p>
        </footer>
    )
};

FooterWrapper.propTypes = {}

