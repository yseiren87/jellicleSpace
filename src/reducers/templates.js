import {} from "../keys";
import {Codes} from "../utils";

const initState = {
    pageCode: null,
}

export const templateReducer = (state = initState, action) => {

    switch (action.type) {
        default:
            return state;
    }

};